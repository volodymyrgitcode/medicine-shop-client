export interface Medicine {
    id: string;
    name: string;
    description: string;
    imageUrl: string;
    price: string;
    stock: number;
    createdAt: string;
    updatedAt: string;
    shopId: string;
}